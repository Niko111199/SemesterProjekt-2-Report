using UnityEngine;

public class SceneSetUpSkattejagtScene : MonoBehaviour
{

    [SerializeField] GPSSystem gpsSystem;
    [SerializeField] NavigationSystem NavigationSystem;
    [SerializeField] Compas compas;



    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
      
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
